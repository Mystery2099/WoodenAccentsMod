package com.github.mystery2099.woodenAccentsMod.item.group

/** Loader-agnostic creative tab identifiers that blocks can opt into. */
enum class ModItemGroup(val path: String) {
    FURNITURE("decorations"),
    STORAGE("miscellaneous"),
    BUILDING("structural_elements");
}
